# import * from './generated/prisma.graphql'

type Mutation {
	createItem(
		title: String
		description: String
		image: String
		maxDays: Int
	): Item!
}

type Query {
	items: [Item]!
	item(where: ItemWhereUniqueInput!): Item
}
